model:
  task: classification
  checkpoint: /CHECKPOINT/backbones/vit/vit-tiny.pth
  architecture:
    full: ~ # auto
    backbone: vit
    head: fc
  losses:
    - criterion: label_smoothing_cross_entropy
      smoothing: 0.1
      weight: ~