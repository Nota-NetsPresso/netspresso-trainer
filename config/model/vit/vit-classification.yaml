model:
  task: classification
  checkpoint: ./weights/vit/vit-tiny.pth
  freeze_backbone: False
  architecture:
    full: ~ # auto
    backbone:
      type: vit
    head:
      type: fc
  losses:
    - criterion: label_smoothing_cross_entropy
      smoothing: 0.1
      weight: ~