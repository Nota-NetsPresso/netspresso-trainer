model:
  task: classification
  checkpoint: /CHECKPOINT/backbones/resnet/resnet50.pth
  architecture:
    full: ~ # auto
    backbone: resnet50
    head: fc
  losses:
    - criterion: label_smoothing_cross_entropy
      smoothing: 0.01
      weight: ~