model:
  task: classification
  checkpoint: ./weights/resnet/resnet50.pth
  freeze_backbone: False
  architecture:
    full: ~ # auto
    backbone:
      type: resnet50
    head:
      type: fc
  losses:
    - criterion: label_smoothing_cross_entropy
      smoothing: 0.1
      weight: ~